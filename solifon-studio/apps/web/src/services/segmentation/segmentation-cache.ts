import{SegmentationResult,SegmentationParameters}from "./segmentation-types";export class SegmentationCache{private static cache=new Map<string,SegmentationResult>();public static canonicalizeParams(params?:SegmentationParameters):string{if (!params) return "";const keys=Object.keys(params).sort();const parts=keys.map(key=>{const val=params[key];if (val===null) return `${key}:null`;return `${key}:${val.toString()}`;});return parts.join("|");}public static generateKey( inputHash:string,modelId:string,modelVersion:string,parameters?:SegmentationParameters ):string{const paramPart=this.canonicalizeParams(parameters);return `input:${inputHash}_model:${modelId}_ver:${modelVersion}_params:[${paramPart}]`;}public static get( inputHash:string,modelId:string,modelVersion:string,parameters?:SegmentationParameters ):SegmentationResult | null{const key=this.generateKey(inputHash,modelId,modelVersion,parameters);return this.cache.get(key) || null;}public static set( inputHash:string,modelId:string,modelVersion:string,parameters:SegmentationParameters | undefined,result:SegmentationResult ):void{const key=this.generateKey(inputHash,modelId,modelVersion,parameters);this.cache.set(key,result);}public static clear():void{this.cache.clear();}public static size():number{return this.cache.size;}}