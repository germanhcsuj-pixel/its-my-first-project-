import{MaskSource}from "@/types/timeline";export interface Bounds{x:number;y:number;width:number;height:number;}export type TrackingGapPolicy="none" | "hold-last";export interface TrackObservation{frameIndex:number;timestamp:number;detectionId:string;label:string;confidence:number;bounds:Bounds;maskSource:MaskSource;}export interface Track{trackId:string;label:string;observations:TrackObservation[];startFrame:number;endFrame:number;}export interface TrackingResult{inputHash:string;modelId:string;modelVersion:string;tracks:Track[];frameCount:number;}export type TrackingParameters=Record<string,string | number | boolean | null>;