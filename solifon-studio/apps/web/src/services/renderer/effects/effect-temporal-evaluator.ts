import{AnimatedNumber,AnimatedRGBA,EffectKeyframe,RGBA}from "../../../types/timeline";export class EffectTemporalEvaluator{public static evaluateNumber( param:AnimatedNumber,t:number,resolvedDependencies:Map<string,number | RGBA> ):number{if (param.mode==="static"){return param.value;}if (param.mode==="keyframes"){const kfs=param.keyframes;if (!kfs || kfs.length===0){throw new Error("Cannot evaluate empty keyframes");}if (t <=kfs[0].time){return kfs[0].value;}if (t >=kfs[kfs.length-1].time){return kfs[kfs.length-1].value;}let k0=kfs[0];let k1=kfs[1];for (let i=0;i < kfs.length-1;i++){if (t >=kfs[i].time && t <=kfs[i+1].time){k0=kfs[i];k1=kfs[i+1];break;}}if (param.interpolation==="step"){return k0.value;}const denominator=k1.time-k0.time;if (denominator===0){return k0.value;}const u=(t-k0.time)/denominator;return k0.value+(k1.value-k0.value)*u;}if (param.mode==="reference"){const refKey=param.parameterId;const refVal=resolvedDependencies.get(refKey);if (refVal===undefined){throw new Error(`Dependency '${refKey}' not found in resolved dependencies`);}if (typeof refVal !=="number"){throw new Error(`Dependency '${refKey}' is not of type number`);}const scale=param.scale !==undefined ? param.scale:1;const offset=param.offset !==undefined ? param.offset:0;return refVal*scale+offset;}throw new Error("Unknown parameter mode");}public static evaluateRGBA( param:AnimatedRGBA,t:number,resolvedDependencies:Map<string,number | RGBA> ):RGBA{if (param.mode==="static"){return param.value;}if (param.mode==="keyframes"){const kfs=param.keyframes;if (!kfs || kfs.length===0){throw new Error("Cannot evaluate empty keyframes");}if (t <=kfs[0].time){return{...kfs[0].value};}if (t >=kfs[kfs.length-1].time){return{...kfs[kfs.length-1].value};}let k0=kfs[0];let k1=kfs[1];for (let i=0;i < kfs.length-1;i++){if (t >=kfs[i].time && t <=kfs[i+1].time){k0=kfs[i];k1=kfs[i+1];break;}}if (param.interpolation==="step"){return{...k0.value};}const denominator=k1.time-k0.time;if (denominator===0){return{...k0.value};}const u=(t-k0.time)/denominator;const r=k0.value.r+(k1.value.r-k0.value.r)*u;const g=k0.value.g+(k1.value.g-k0.value.g)*u;const b=k0.value.b+(k1.value.b-k0.value.b)*u;const a=k0.value.a+(k1.value.a-k0.value.a)*u;return{r,g,b,a};}if (param.mode==="reference"){const refKey=param.parameterId;const refVal=resolvedDependencies.get(refKey);if (refVal===undefined){throw new Error(`Dependency '${refKey}' not found in resolved dependencies`);}if (typeof refVal==="number"){throw new Error(`Dependency '${refKey}' is number but expected RGBA`);}return{...refVal};}throw new Error("Unknown parameter mode");}}