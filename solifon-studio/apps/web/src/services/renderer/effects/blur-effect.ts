import type{RenderEffect,EffectContext}from "./render-effect";import type{RenderTarget}from "../render-target";export class BlurEffect implements RenderEffect{public type="blur";constructor(public intensity:number=0){}apply(input:RenderTarget,output:RenderTarget,context:EffectContext):void{if (this.intensity <=0){output.context.drawImage(input.canvas,0,0);return;}output.context.save();output.context.filter=`blur(${this.intensity}px)`;output.context.drawImage(input.canvas,0,0);output.context.restore();}}