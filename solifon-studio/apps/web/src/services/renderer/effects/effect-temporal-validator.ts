import{AnimatedNumber,AnimatedRGBA,EffectKeyframe,RGBA}from "../../../types/timeline";export class EffectTemporalValidator{public static validateParameterKey(key:string):void{if (!key || typeof key !=="string"){throw new Error("Parameter key must be a non-empty string");}const parts=key.split(".");if (parts.length !==2){throw new Error(`Invalid ParameterKey format:'${key}'. Expected 'effectId.parameterId'`);}const [effectId,parameterId]=parts;if (!effectId || effectId.trim()===""){throw new Error(`Invalid ParameterKey '${key}':effectId cannot be empty`);}if (!parameterId || parameterId.trim()===""){throw new Error(`Invalid ParameterKey '${key}':parameterId cannot be empty`);}}public static validateNumber( effectId:string,paramName:string,param:AnimatedNumber,min?:number,max?:number,strictlyPositive?:boolean ):void{if (!param){throw new Error(`[${effectId}] Parameter '${paramName}' is missing or undefined`);}if (param.mode==="static"){const v=param.value;if (typeof v !=="number" || isNaN(v) || !isFinite(v)){throw new Error(`[${effectId}] Parameter '${paramName}':static value is invalid (not finite number)`);}if (min !==undefined && v < min){throw new Error(`[${effectId}] Parameter '${paramName}':static value ${v}is less than min ${min}`);}if (max !==undefined && v > max){throw new Error(`[${effectId}] Parameter '${paramName}':static value ${v}is greater than max ${max}`);}if (strictlyPositive && v <=0){throw new Error(`[${effectId}] Parameter '${paramName}':static value ${v}must be strictly positive`);}return;}if (param.mode==="keyframes"){if (!param.keyframes || param.keyframes.length===0){throw new Error(`[${effectId}] Parameter '${paramName}':keyframes array cannot be empty`);}if (param.interpolation !=="step" && param.interpolation !=="linear"){throw new Error(`[${effectId}] Parameter '${paramName}':invalid interpolation mode '${param.interpolation}'`);}let lastTime=-1;for (let i=0;i < param.keyframes.length;i++){const kf=param.keyframes[i];if (!kf){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}is null or undefined`);}if (typeof kf.time !=="number" || isNaN(kf.time) || !isFinite(kf.time) || kf.time < 0){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:invalid time ${kf?.time}`);}if (kf.time <=lastTime){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:keyframe times must be strictly increasing`);}lastTime=kf.time;const v=kf.value;if (typeof v !=="number" || isNaN(v) || !isFinite(v)){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:value ${v}is invalid (not finite number)`);}if (min !==undefined && v < min){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:value ${v}is less than min ${min}`);}if (max !==undefined && v > max){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:value ${v}is greater than max ${max}`);}if (strictlyPositive && v <=0){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:value ${v}must be strictly positive`);}}return;}if (param.mode==="reference"){this.validateParameterKey(param.parameterId);if (param.scale !==undefined && (typeof param.scale !=="number" || isNaN(param.scale) || !isFinite(param.scale))){throw new Error(`[${effectId}] Parameter '${paramName}':scale must be a finite number`);}if (param.offset !==undefined && (typeof param.offset !=="number" || isNaN(param.offset) || !isFinite(param.offset))){throw new Error(`[${effectId}] Parameter '${paramName}':offset must be a finite number`);}return;}throw new Error(`[${effectId}] Parameter '${paramName}':unknown mode '${(param as{mode?:string}).mode}'`);}public static validateRGBA( effectId:string,paramName:string,param:AnimatedRGBA ):void{if (!param){throw new Error(`[${effectId}] Parameter '${paramName}' is missing or undefined`);}if (param.mode==="static"){this.validateColor(effectId,paramName,-1,param.value);return;}if (param.mode==="keyframes"){if (!param.keyframes || param.keyframes.length===0){throw new Error(`[${effectId}] Parameter '${paramName}':keyframes array cannot be empty`);}if (param.interpolation !=="step" && param.interpolation !=="linear"){throw new Error(`[${effectId}] Parameter '${paramName}':invalid interpolation mode '${param.interpolation}'`);}let lastTime=-1;for (let i=0;i < param.keyframes.length;i++){const kf=param.keyframes[i];if (!kf){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}is null or undefined`);}if (typeof kf.time !=="number" || isNaN(kf.time) || !isFinite(kf.time) || kf.time < 0){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:invalid time ${kf?.time}`);}if (kf.time <=lastTime){throw new Error(`[${effectId}] Parameter '${paramName}' at keyframe index ${i}:keyframe times must be strictly increasing`);}lastTime=kf.time;this.validateColor(effectId,paramName,i,kf.value);}return;}if (param.mode==="reference"){this.validateParameterKey(param.parameterId);return;}throw new Error(`[${effectId}] Parameter '${paramName}':unknown mode '${(param as{mode?:string}).mode}'`);}public static validateColor(effectId:string,paramName:string,index:number,color:RGBA):void{if (!color){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color object is missing`);}const fields:("r" | "g" | "b" | "a")[]=["r","g","b","a"];for (const f of fields){const v=color[f];if (typeof v !=="number" || isNaN(v) || !isFinite(v)){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color field '${f}' is invalid (not finite number)`);}}if (color.r < 0 || color.r > 255){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color red channel out of bounds [0,255]`);}if (color.g < 0 || color.g > 255){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color green channel out of bounds [0,255]`);}if (color.b < 0 || color.b > 255){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color blue channel out of bounds [0,255]`);}if (color.a < 0 || color.a > 1){throw new Error(`[${effectId}] Parameter '${paramName}'${index >=0 ? ` at keyframe ${index}`:""}:color alpha channel out of bounds [0,1]`);}}public static validateResolvedBounds( effectId:string,paramName:string,v:number,min?:number,max?:number,strictlyPositive?:boolean ):void{if (typeof v !=="number" || isNaN(v) || !isFinite(v)){throw new Error(`[${effectId}] Parameter '${paramName}':resolved value is invalid (not finite number)`);}if (min !==undefined && v < min){throw new Error(`[${effectId}] Parameter '${paramName}':resolved value ${v}is less than min ${min}`);}if (max !==undefined && v > max){throw new Error(`[${effectId}] Parameter '${paramName}':resolved value ${v}is greater than max ${max}`);}if (strictlyPositive && v <=0){throw new Error(`[${effectId}] Parameter '${paramName}':resolved value ${v}must be strictly positive`);}}}