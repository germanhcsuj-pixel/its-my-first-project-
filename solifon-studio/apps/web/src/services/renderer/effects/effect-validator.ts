import{EffectDefinition}from "@/types/timeline";import{EffectKeyframeValidator}from "./effect-keyframe-validator";export class EffectValidator{public static validate(effect:EffectDefinition):void{if (!effect){throw new Error("Effect definition is required");}if (!effect.id || typeof effect.id !=="string" || effect.id.trim()===""){throw new Error("Effect id must be a non-empty string");}if (typeof effect.enabled !=="boolean"){throw new Error("Effect enabled must be a boolean");}if (typeof effect.opacity !=="number" || isNaN(effect.opacity) || !isFinite(effect.opacity) || effect.opacity < 0 || effect.opacity > 1){throw new Error(`Effect opacity must be a number between 0 and 1,got:${effect.opacity}`);}if (effect.target){const{type}=effect.target;if (type !=="layer" && type !=="mask" && type !=="track"){throw new Error(`Invalid effect target type:${type}`);}if (type==="layer"){if (!effect.target.elementId || typeof effect.target.elementId !=="string"){throw new Error("Layer target elementId must be a non-empty string");}}else if (type==="mask"){if (!effect.target.maskId || typeof effect.target.maskId !=="string"){throw new Error("Mask target maskId must be a non-empty string");}}else if (type==="track"){if (!effect.target.trackId || typeof effect.target.trackId !=="string"){throw new Error("Track target trackId must be a non-empty string");}}}if (effect.type==="blur"){const{radius,quality}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"radius",radius,0);if (quality !==undefined){if (typeof quality !=="number" || isNaN(quality) || !isFinite(quality) || quality <=0){throw new Error(`Blur quality must be a positive finite number when present,got:${quality}`);}}}else if (effect.type==="glow"){const{radius,intensity,color}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"radius",radius,0);EffectKeyframeValidator.validateNumber(effect.id,"intensity",intensity,0);EffectKeyframeValidator.validateRGBA(effect.id,"color",color);}else if (effect.type==="color"){const{brightness,contrast,saturation,hue}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"brightness",brightness,0);EffectKeyframeValidator.validateNumber(effect.id,"contrast",contrast,0);EffectKeyframeValidator.validateNumber(effect.id,"saturation",saturation,0);EffectKeyframeValidator.validateNumber(effect.id,"hue",hue);}else if (effect.type==="displacement"){const{strength,scale,angle}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"strength",strength,-1000,1000);EffectKeyframeValidator.validateNumber(effect.id,"scale",scale,0,1000,true);EffectKeyframeValidator.validateNumber(effect.id,"angle",angle,0,360);}else if (effect.type==="wave"){const{amplitude,frequency,phase,direction}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"amplitude",amplitude,0,1000);EffectKeyframeValidator.validateNumber(effect.id,"frequency",frequency,0,1000,true);EffectKeyframeValidator.validateNumber(effect.id,"phase",phase,-1000,1000);EffectKeyframeValidator.validateNumber(effect.id,"direction",direction,0,360);}else if (effect.type==="lens"){const{strength,radius,centerX,centerY}=effect.parameters;EffectKeyframeValidator.validateNumber(effect.id,"strength",strength,-10,10);EffectKeyframeValidator.validateNumber(effect.id,"radius",radius,0,10000,true);EffectKeyframeValidator.validateNumber(effect.id,"centerX",centerX,-10000,10000);EffectKeyframeValidator.validateNumber(effect.id,"centerY",centerY,-10000,10000);}else{throw new Error(`Unsupported effect type:${(effect as{type:string}).type}`);}}}