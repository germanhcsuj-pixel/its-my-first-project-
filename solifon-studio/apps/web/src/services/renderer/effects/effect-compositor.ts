import{RenderTarget}from "../render-target";import{EvaluatedEffect,MaskDefinition,EvaluatedDisplacementEffectParams,EvaluatedWaveEffectParams,EvaluatedLensEffectParams}from "@/types/timeline";import{RenderTargetPool}from "../masks/mask-compositor";import{MaskEvaluator}from "../masks/mask-evaluator";import{MaskRenderer}from "../masks/mask-renderer";import{EffectReference,PixelBuffer}from "./effect-reference";export class EffectCompositor{public static applyEffects( sourceTarget:RenderTarget,effects:EvaluatedEffect[] ):void{const enabledEffects=effects.filter(e=> e.enabled);if (enabledEffects.length===0){return;}const width=sourceTarget.width;const height=sourceTarget.height;for (const effect of enabledEffects){const tempProcessed=RenderTargetPool.acquire(width,height);try{const srcImgData=sourceTarget.context.getImageData(0,0,width,height);const originalPixels=new Uint8ClampedArray(srcImgData.data);const pixelBuffer:PixelBuffer={width,height,data:new Uint8ClampedArray(srcImgData.data)};let processed:PixelBuffer;if (effect.type==="blur"){processed=EffectReference.blur(pixelBuffer,effect.parameters.radius);}else if (effect.type==="glow"){processed=EffectReference.glow(pixelBuffer,effect.parameters.radius,effect.parameters.intensity,effect.parameters.color);}else if (effect.type==="color"){processed=EffectReference.color(pixelBuffer,effect.parameters.brightness,effect.parameters.contrast,effect.parameters.saturation,effect.parameters.hue);}else if (effect.type==="displacement"){processed=this.applyDisplacement(pixelBuffer,effect.parameters);}else if (effect.type==="wave"){processed=this.applyWave(pixelBuffer,effect.parameters);}else{processed=this.applyLens(pixelBuffer,effect.parameters);}const processedPixels=processed.data;const finalData=srcImgData.data;const target=effect.target;const opacity=effect.opacity;if (target.type==="mask" || target.type==="track"){const maskSource=target.source;const maskId=target.type==="mask" ? target.maskId:target.trackId;const dummyMaskDef:MaskDefinition={id:maskId,source:maskSource,mode:"add",inverted:false,feather:0,opacity:1};const evaluatedMask=MaskEvaluator.evaluate(dummyMaskDef,0);const maskCanvas=MaskRenderer.renderToCanvas(evaluatedMask,width,height);const maskCtx=(maskCanvas as OffscreenCanvas).getContext("2d");if (!maskCtx){throw new Error("Failed to get 2D context for mask canvas");}const maskData=maskCtx.getImageData(0,0,width,height);const maskPixels=maskData.data;for (let i=0;i < width*height;i++){const idx=i*4;const maskVal=maskPixels[idx+3]/255;const factor=opacity*maskVal;if (factor > 0){finalData[idx]=Math.max(0,Math.min(255,Math.round(originalPixels[idx]*(1-factor)+processedPixels[idx]*factor)));finalData[idx+1]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+1]*(1-factor)+processedPixels[idx+1]*factor)));finalData[idx+2]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+2]*(1-factor)+processedPixels[idx+2]*factor)));finalData[idx+3]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+3]*(1-factor)+processedPixels[idx+3]*factor)));}else{finalData[idx]=originalPixels[idx];finalData[idx+1]=originalPixels[idx+1];finalData[idx+2]=originalPixels[idx+2];finalData[idx+3]=originalPixels[idx+3];}}}else{for (let i=0;i < width*height;i++){const idx=i*4;finalData[idx]=Math.max(0,Math.min(255,Math.round(originalPixels[idx]*(1-opacity)+processedPixels[idx]*opacity)));finalData[idx+1]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+1]*(1-opacity)+processedPixels[idx+1]*opacity)));finalData[idx+2]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+2]*(1-opacity)+processedPixels[idx+2]*opacity)));finalData[idx+3]=Math.max(0,Math.min(255,Math.round(originalPixels[idx+3]*(1-opacity)+processedPixels[idx+3]*opacity)));}}sourceTarget.context.putImageData(srcImgData,0,0);}finally{RenderTargetPool.release(tempProcessed);}}}private static sampleBilinear(buf:PixelBuffer,u:number,v:number){const{width,height,data}=buf;if (u < 0 || u > width-1 || v < 0 || v > height-1){return{r:0,g:0,b:0,a:0};}const x1=Math.floor(u);const y1=Math.floor(v);const x2=Math.min(width-1,x1+1);const y2=Math.min(height-1,y1+1);const dx=u-x1;const dy=v-y1;const idx11=(y1*width+x1)*4;const idx21=(y1*width+x2)*4;const idx12=(y2*width+x1)*4;const idx22=(y2*width+x2)*4;const w11=(1-dx)*(1-dy);const w21=dx*(1-dy);const w12=(1-dx)*dy;const w22=dx*dy;return{r:Math.round(data[idx11]*w11+data[idx21]*w21+data[idx12]*w12+data[idx22]*w22),g:Math.round(data[idx11+1]*w11+data[idx21+1]*w21+data[idx12+1]*w12+data[idx22+1]*w22),b:Math.round(data[idx11+2]*w11+data[idx21+2]*w21+data[idx12+2]*w12+data[idx22+2]*w22),a:Math.round(data[idx11+3]*w11+data[idx21+3]*w21+data[idx12+3]*w12+data[idx22+3]*w22),};}private static applyDisplacement(src:PixelBuffer,params:EvaluatedDisplacementEffectParams):PixelBuffer{if (typeof params.strength !=="number" || isNaN(params.strength) || !isFinite(params.strength) || typeof params.scale !=="number" || isNaN(params.scale) || !isFinite(params.scale) || params.scale <=0 || typeof params.angle !=="number" || isNaN(params.angle) || !isFinite(params.angle)){throw new Error("Invalid displacement parameters");}const{width,height}=src;const outData=new Uint8ClampedArray(width*height*4);const outBuf:PixelBuffer={width,height,data:outData};const{strength,scale,angle}=params;if (strength===0){outData.set(src.data);return outBuf;}const rad=(angle*Math.PI)/180;const cosA=Math.cos(rad);const sinA=Math.sin(rad);for (let y=0;y < height;y++){for (let x=0;x < width;x++){const u=x+strength*cosA*Math.sin((y/scale)*2*Math.PI);const v=y+strength*sinA*Math.sin((x/scale)*2*Math.PI);const sampled=this.sampleBilinear(src,u,v);const outIdx=(y*width+x)*4;outData[outIdx]=sampled.r;outData[outIdx+1]=sampled.g;outData[outIdx+2]=sampled.b;outData[outIdx+3]=sampled.a;}}return outBuf;}private static applyWave(src:PixelBuffer,params:EvaluatedWaveEffectParams):PixelBuffer{if (typeof params.amplitude !=="number" || isNaN(params.amplitude) || !isFinite(params.amplitude) || params.amplitude < 0 || typeof params.frequency !=="number" || isNaN(params.frequency) || !isFinite(params.frequency) || params.frequency <=0 || typeof params.phase !=="number" || isNaN(params.phase) || !isFinite(params.phase) || typeof params.direction !=="number" || isNaN(params.direction) || !isFinite(params.direction)){throw new Error("Invalid wave parameters");}const{width,height}=src;const outData=new Uint8ClampedArray(width*height*4);const outBuf:PixelBuffer={width,height,data:outData};const{amplitude,frequency,phase,direction}=params;if (amplitude===0){outData.set(src.data);return outBuf;}const rad=(direction*Math.PI)/180;const cosD=Math.cos(rad);const sinD=Math.sin(rad);for (let y=0;y < height;y++){for (let x=0;x < width;x++){const p=x*cosD+y*sinD;const waveOffset=Math.sin(p*frequency*2*Math.PI+phase)*amplitude;const u=x-sinD*waveOffset;const v=y+cosD*waveOffset;const sampled=this.sampleBilinear(src,u,v);const outIdx=(y*width+x)*4;outData[outIdx]=sampled.r;outData[outIdx+1]=sampled.g;outData[outIdx+2]=sampled.b;outData[outIdx+3]=sampled.a;}}return outBuf;}private static applyLens(src:PixelBuffer,params:EvaluatedLensEffectParams):PixelBuffer{if (typeof params.strength !=="number" || isNaN(params.strength) || !isFinite(params.strength) || params.strength <-10 || params.strength > 10 || typeof params.radius !=="number" || isNaN(params.radius) || !isFinite(params.radius) || params.radius <=0 || typeof params.centerX !=="number" || isNaN(params.centerX) || !isFinite(params.centerX) || typeof params.centerY !=="number" || isNaN(params.centerY) || !isFinite(params.centerY)){throw new Error("Invalid lens parameters");}const{width,height}=src;const outData=new Uint8ClampedArray(width*height*4);const outBuf:PixelBuffer={width,height,data:outData};const{strength,radius,centerX,centerY}=params;if (strength===0){outData.set(src.data);return outBuf;}for (let y=0;y < height;y++){for (let x=0;x < width;x++){const dx=x-centerX;const dy=y-centerY;const r=Math.sqrt(dx*dx+dy*dy);if (r < radius && r > 0){const t=r/radius;const distortedT=Math.pow(t,strength > 0 ? (1/(1+strength)):(1-strength));const u=centerX+dx*(distortedT/t);const v=centerY+dy*(distortedT/t);const sampled=this.sampleBilinear(src,u,v);const outIdx=(y*width+x)*4;outData[outIdx]=sampled.r;outData[outIdx+1]=sampled.g;outData[outIdx+2]=sampled.b;outData[outIdx+3]=sampled.a;}else{const idx=(y*width+x)*4;outData[idx]=src.data[idx];outData[idx+1]=src.data[idx+1];outData[idx+2]=src.data[idx+2];outData[idx+3]=src.data[idx+3];}}}return outBuf;}}