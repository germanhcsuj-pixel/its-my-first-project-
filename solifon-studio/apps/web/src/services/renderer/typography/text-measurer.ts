import{fontRegistry,FontSpec}from "./font-registry";export interface TextMeasureSpec{fontFamily:string;fontSize:number;fontWeight?:string | number;fontStyle?:string;}export class TextMeasurer{private static instance:TextMeasurer;private ctx:CanvasRenderingContext2D | OffscreenCanvasRenderingContext2D;private constructor(){if (typeof OffscreenCanvas !=="undefined"){const canvas=new OffscreenCanvas(1,1);this.ctx=canvas.getContext("2d") as OffscreenCanvasRenderingContext2D;}else if (typeof document !=="undefined"){const canvas=document.createElement("canvas");canvas.width=1;canvas.height=1;this.ctx=canvas.getContext("2d") as CanvasRenderingContext2D;}else{this.ctx={measureText:(text:string)=> ({width:text.length*10}),font:''}as any;}}public static getInstance():TextMeasurer{if (!TextMeasurer.instance){TextMeasurer.instance=new TextMeasurer();}return TextMeasurer.instance;}private getFontString(spec:TextMeasureSpec):string{const weight=spec.fontWeight ?? "normal";const style=spec.fontStyle ?? "normal";return `${style}${weight}${spec.fontSize}px "${spec.fontFamily}"`;}public measureText(text:string,spec:TextMeasureSpec):TextMetrics{const fontSpec:FontSpec={family:spec.fontFamily,weight:spec.fontWeight,style:spec.fontStyle,};if (!fontRegistry.isReady(fontSpec)){console.warn(`TextMeasurer:font ${fontRegistry.getFontKey(fontSpec)}is not ready. Measurements may be incorrect.`);}this.ctx.font=this.getFontString(spec);return this.ctx.measureText(text);}}export const textMeasurer=TextMeasurer.getInstance();