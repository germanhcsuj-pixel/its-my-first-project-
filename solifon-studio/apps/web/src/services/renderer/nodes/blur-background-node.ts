import type{RenderTarget}from "../render-target";import{BaseNode}from "./base-node";export type BlurBackgroundNodeParams={blurIntensity:number;contentNodes:BaseNode[];};export class BlurBackgroundNode extends BaseNode<BlurBackgroundNodeParams>{private blurIntensity:number;private contentNodes:BaseNode[];constructor(params:BlurBackgroundNodeParams){super(params);this.blurIntensity=params.blurIntensity;this.contentNodes=params.contentNodes;}async render({target,time,}:{target:RenderTarget;time:number;}):Promise<void>{let offscreen:OffscreenCanvas | HTMLCanvasElement;let offscreenCtx:| OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D;try{offscreen=new OffscreenCanvas(target.width,target.height);const ctx=offscreen.getContext("2d");if (!ctx){throw new Error("failed to get offscreen canvas context");}offscreenCtx=ctx;}catch{offscreen=document.createElement("canvas");offscreen.width=target.width;offscreen.height=target.height;const ctx=offscreen.getContext("2d");if (!ctx){throw new Error("failed to get canvas context");}offscreenCtx=ctx;}const originalContext=target.context;target.context=offscreenCtx;for (const node of this.contentNodes){await node.render({target,time});}target.context=originalContext;const zoomScale=1.4;const scaledWidth=target.width*zoomScale;const scaledHeight=target.height*zoomScale;const offsetX=(target.width-scaledWidth)/2;const offsetY=(target.height-scaledHeight)/2;target.context.save();target.context.filter=`blur(${this.blurIntensity}px)`;target.context.drawImage( offscreen as CanvasImageSource,offsetX,offsetY,scaledWidth,scaledHeight,);target.context.restore();}}