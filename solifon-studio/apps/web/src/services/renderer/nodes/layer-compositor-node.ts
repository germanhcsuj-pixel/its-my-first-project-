import{BaseNode}from "./base-node";import type{RenderTarget}from "../render-target";import{applyBlendMode}from "../blend-modes";import{getSharedTargets}from "../shared-targets";export class LayerCompositorNode extends BaseNode{async render(args:{target:RenderTarget;time:number;forceRender?:boolean;skipEffects?:Set<string>}){const sortedChildren=[...this.children].sort((a,b)=>{if (a.zIndex !==b.zIndex){return a.zIndex-b.zIndex;}return a.id.localeCompare(b.id);});const{tempTarget,targetB}=getSharedTargets(args.target.width,args.target.height);for (const child of sortedChildren){tempTarget.clear();if ((args as any).scheduler){await (args as any).scheduler.renderNode({node:child,target:tempTarget,time:args.time,forceRender:args.forceRender});}else{await child.render({...args,target:tempTarget,ignoreOpacity:true}as any);}let currentTarget=tempTarget;const effectsToApply=child.effects?.filter(e=> !args.skipEffects?.has(e.type)) || [];if (effectsToApply.length > 0){const context={time:args.time,width:args.target.width,height:args.target.height,deltaTime:0,fps:(args.target as any).fps ?? 30,renderNodeAtTime:async (t:number,target:RenderTarget)=>{target.clear();if ((args as any).scheduler){await (args as any).scheduler.renderNode({node:child,target,time:t,forceRender:args.forceRender});}else{await child.render({...args,time:t,target,ignoreOpacity:true}as any);}}};let nextTarget=targetB;for (const effect of effectsToApply){nextTarget.clear();const result=effect.apply(currentTarget,nextTarget,context);if (result instanceof Promise){await result;}const t=currentTarget;currentTarget=nextTarget;nextTarget=t;}}const opacity=typeof (child as any).getOpacity==='function' ? (child as any).getOpacity(args.time):1;const blendMode=child.blendMode || "normal";args.target.context.save();args.target.context.globalAlpha=opacity;applyBlendMode(args.target.context,blendMode);args.target.context.drawImage(currentTarget.canvas,0,0);args.target.context.restore();}}}