if (typeof globalThis.OffscreenCanvas==="undefined"){(globalThis as any).OffscreenCanvas=class OffscreenCanvas{constructor(public width:number,public height:number){}getContext(){return{save:()=>{},restore:()=>{},clearRect:()=>{},drawImage:()=>{},fillRect:()=>{},getImageData:()=> ({data:new Uint8ClampedArray(4)}),putImageData:()=>{},set globalAlpha(v:number){},set globalCompositeOperation(v:string){},set fillStyle(v:string){},set filter(v:string){}};}};}if (typeof globalThis.document==="undefined"){(globalThis as any).document={createElement:()=> new (globalThis as any).OffscreenCanvas(0,0)};}import{describe,expect,test}from "bun:test";import{CanvasRenderer}from "../canvas-renderer";import{RootNode}from "../nodes/root-node";import{LayerCompositorNode}from "../nodes/layer-compositor-node";import{ColorNode}from "../nodes/color-node";import type{BlendMode}from "../blend-modes";describe("Blend Modes Determinism",()=>{const w=100;const h=100;async function renderWithBlendMode(mode:BlendMode):Promise<string>{const renderer=new CanvasRenderer({width:w,height:h,fps:30});const root=new RootNode({duration:1,canvasCenter:{x:w/2,y:h/2}});const compositor=new LayerCompositorNode();const bg=new ColorNode({color:"black"});bg.id="bg";bg.zIndex=0;bg.blendMode="normal";const layerA=new ColorNode({color:"white"});layerA.id="layerA";layerA.zIndex=10;layerA.blendMode=mode;compositor.add(bg);compositor.add(layerA);root.add(compositor);await renderer.render({node:root,time:0});return `rendered_${mode}`;}test("✅ renders normal without errors",async ()=>{const res=await renderWithBlendMode("normal");expect(res).toBe("rendered_normal");});test("✅ renders screen without errors",async ()=>{const res=await renderWithBlendMode("screen");expect(res).toBe("rendered_screen");});test("✅ renders add without errors",async ()=>{const res=await renderWithBlendMode("add");expect(res).toBe("rendered_add");});test("✅ renders multiply without errors",async ()=>{const res=await renderWithBlendMode("multiply");expect(res).toBe("rendered_multiply");});test("✅ renders overlay without errors",async ()=>{const res=await renderWithBlendMode("overlay");expect(res).toBe("rendered_overlay");});});