export function apply2DEffect({ctx,effectId,time,drawElement,}:{ctx:CanvasRenderingContext2D | OffscreenCanvasRenderingContext2D;effectId:string;time:number;drawElement:()=> void;}){ctx.save();switch (effectId){case "3d-god-rays":{ctx.save();ctx.globalCompositeOperation="screen";const raysCount=10;const lightX=Math.sin(time*1.2)*25;const lightY=-35;for (let i=raysCount;i > 0;i--){ctx.save();ctx.globalAlpha=(1-i/raysCount)*0.25;ctx.translate((lightX*i)/4,(lightY*i)/4);ctx.filter=`blur(${i*2.5}px) drop-shadow(0 0 12px #00aaff)`;drawElement();ctx.restore();}ctx.restore();drawElement();break;}case "3d-parallax-extrusion":{const depth=12;const shiftX=Math.cos(time*1.5)*2;const shiftY=Math.sin(time*1.5)*2;for (let i=depth;i > 0;i--){ctx.save();ctx.translate(i*shiftX,i*shiftY);ctx.filter=`brightness(40%) sepia(100%) hue-rotate(190deg)`;drawElement();ctx.restore();}drawElement();break;}case "3d-mercury-fluid":{ctx.save();drawElement();ctx.globalCompositeOperation="source-atop";const gradX=Math.sin(time*2)*150;const gradY=Math.cos(time*2)*150;const gradient=ctx.createLinearGradient(-100+gradX,-100+gradY,300+gradX,300+gradY );gradient.addColorStop(0,"#e0e0e0");gradient.addColorStop(0.25,"#ffffff");gradient.addColorStop(0.5,"#1e40af");gradient.addColorStop(0.75,"#ffffff");gradient.addColorStop(1,"#111111");ctx.fillStyle=gradient;ctx.fillRect(-1000,-1000,3000,3000);ctx.restore();break;}default:drawElement();break;}ctx.restore();}