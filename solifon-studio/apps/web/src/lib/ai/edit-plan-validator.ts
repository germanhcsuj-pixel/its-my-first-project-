import type{AIEditPlan,Cut,Transition}from "./edit-plan";export type ValidationErrorCode=| "OVERLAPPING_CLIPS" | "INVALID_MEDIA_ID" | "NEGATIVE_TIMESTAMP" | "CLIP_EXCEEDS_MEDIA_DURATION" | "TRANSITION_TOO_LONG" | "DUPLICATE_CUTS" | "ZERO_DURATION_CLIP" | "TRANSITION_COLLISION" | "FILTER_PARAM_OUT_OF_RANGE" | "CAPTION_OVERFLOW" | "UNSUPPORTED_RENDERER_CAPABILITY" | "TARGET_DURATION_EXCEEDED" | "PLAN_OUTDATED";export type ValidationWarningCode=| "CLIP_TOO_SHORT" | "HIGH_CUT_FREQUENCY" | "DURATION_OVER_TARGET";export type ValidationError={code:ValidationErrorCode;message:string;context?:Record<string,unknown>;};export type ValidationWarning={code:ValidationWarningCode;message:string;context?:Record<string,unknown>;};export type AutoFix={description:string;type:"technical";};export type ValidationResult={valid:boolean;errors:ValidationError[];warnings:ValidationWarning[];autoFixes:AutoFix[];};export type ValidatorContext={mediaLibrary:Map<string,{duration:number}>;currentTimelineRevision:number;rendererCapabilities?:{tier:"high" | "balanced" | "compatibility";supportedCodecs:string[];maxResolution:{width:number;height:number};};};export class EditPlanValidator{validate(plan:AIEditPlan,ctx:ValidatorContext):ValidationResult{const errors:ValidationError[]=[];const warnings:ValidationWarning[]=[];const autoFixes:AutoFix[]=[];if (plan.baseTimelineRevision !==ctx.currentTimelineRevision){errors.push({code:"PLAN_OUTDATED",message:`Plan was built on timeline revision ${plan.baseTimelineRevision},but current revision is ${ctx.currentTimelineRevision}. Regenerate diff before applying.`,context:{planRevision:plan.baseTimelineRevision,currentRevision:ctx.currentTimelineRevision,},});return{valid:false,errors,warnings,autoFixes};}for (const clip of plan.sourceClips){if (!ctx.mediaLibrary.has(clip.mediaId)){errors.push({code:"INVALID_MEDIA_ID",message:`Media ID "${clip.mediaId}" not found in library.`,context:{mediaId:clip.mediaId},});}}const cutTimes=new Set<number>();for (const cut of plan.cuts){if (cut.time < 0){errors.push({code:"NEGATIVE_TIMESTAMP",message:`Cut at negative time:${cut.time}s`,context:{cut},});continue;}const key=Math.round(cut.time*1000);if (cutTimes.has(key)){errors.push({code:"DUPLICATE_CUTS",message:`Duplicate cut at ${cut.time}s`,context:{time:cut.time},});}cutTimes.add(key);}if (plan.cuts.length > 0 && plan.intent){const sorted=[...plan.cuts].sort((a,b)=> a.time-b.time);const constraints=this.getCutConstraintsForPacing(plan.intent.pacing);for (let i=1;i < sorted.length;i++){const interval=sorted[i].time-sorted[i-1].time;if (interval < constraints.minClipDuration){warnings.push({code:"CLIP_TOO_SHORT",message:`Clip between ${sorted[i-1].time}s and ${sorted[i].time}s is ${interval.toFixed(3)}s — below style minimum of ${constraints.minClipDuration}s.`,context:{interval,min:constraints.minClipDuration},});}}if (sorted.length > 0){const span=sorted[sorted.length-1].time-sorted[0].time;const cutsPerSecond=span > 0 ? sorted.length/span:0;if (cutsPerSecond > constraints.maxCutsPerSecond){warnings.push({code:"HIGH_CUT_FREQUENCY",message:`${cutsPerSecond.toFixed(2)}cuts/sec exceeds style max of ${constraints.maxCutsPerSecond}.`,context:{cutsPerSecond,max:constraints.maxCutsPerSecond},});}}}const sortedCuts=[...plan.cuts].sort((a,b)=> a.time-b.time);for (const transition of plan.transitions){if (transition.atTime < 0){errors.push({code:"NEGATIVE_TIMESTAMP",message:`Transition at negative time:${transition.atTime}s`,context:{transition},});continue;}if (transition.duration <=0){errors.push({code:"ZERO_DURATION_CLIP",message:`Transition at ${transition.atTime}s has zero or negative duration.`,context:{transition},});continue;}const nearestCut=this.findNearestCut( sortedCuts.filter(c=> Math.abs(c.time-transition.atTime) > 0.001),transition.atTime );if (nearestCut){const available=Math.abs(transition.atTime-nearestCut.time);if (transition.duration > available*2){const fixedDuration=Math.max(0.1,available);autoFixes.push({description:`Clamped transition at ${transition.atTime}s from ${transition.duration}s to ${fixedDuration.toFixed(2)}s (technical:exceeds clip boundary).`,type:"technical",});transition.duration=fixedDuration;}}}for (const trackEffect of plan.effects){for (const filter of trackEffect.filters || []){if (filter.intensity < 0 || filter.intensity > 1){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Filter "${filter.id}" intensity ${filter.intensity}is out of range [0,1].`,context:{filter},});}if (filter.parameters){const p=filter.parameters;if (p.noise !==undefined && (p.noise < 0 || p.noise > 1)){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Filter "${filter.id}" noise ${p.noise}out of range [0,1].`,});}if (p.distortion !==undefined && (p.distortion < 0 || p.distortion > 1)){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Filter "${filter.id}" distortion ${p.distortion}out of range [0,1].`,});}}}for (const effect of trackEffect.effects || []){if (effect.type==="glow"){if (effect.radius < 0){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Glow radius ${effect.radius}< 0`});}if (effect.intensity < 0 || effect.intensity > 10){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Glow intensity ${effect.intensity}out of range [0,10]`});}if (effect.blendMode && effect.blendMode !=="add" && effect.blendMode !=="screen"){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Unknown glow blend mode:${effect.blendMode}`});}}else if (effect.type==="motion-blur"){if (effect.samples < 1 || effect.samples > 16){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Motion blur samples ${effect.samples}out of range [1,16]`});}if (effect.shutterAngle < 0 || effect.shutterAngle > 360){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Motion blur shutterAngle ${effect.shutterAngle}out of range [0,360]`});}}else if (effect.type==="blur"){if (effect.radius < 0){errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Blur radius ${effect.radius}< 0`});}}else{errors.push({code:"FILTER_PARAM_OUT_OF_RANGE",message:`Unknown effect type:${(effect as any).type}`});}}}if (plan.intent.targetDuration){const actualCutCount=plan.cuts.length;if (actualCutCount===0 && plan.intent.targetDuration > 0){warnings.push({code:"DURATION_OVER_TARGET",message:"No cuts defined but targetDuration is set — final duration may not match target.",});}}if (ctx.rendererCapabilities && ctx.rendererCapabilities.tier==="compatibility"){const hasHeavyFilters=plan.effects.some(e=> e.filters && e.filters.some(f=> ["glitch","vhs","cyberpunk"].includes(f.id)) );if (hasHeavyFilters){warnings.push({code:"HIGH_CUT_FREQUENCY",message:"Heavy WebGL filters (glitch,vhs,cyberpunk) may not render correctly in compatibility mode.",context:{tier:"compatibility"},});}}return{valid:errors.length===0,errors,warnings,autoFixes,};}private getCutConstraintsForPacing(pacing:"slow" | "medium" | "fast"){switch (pacing){case "slow":return{minClipDuration:1.5,maxCutsPerSecond:0.5,preferredCutInterval:3.0};case "medium":return{minClipDuration:0.4,maxCutsPerSecond:1.5,preferredCutInterval:1.5};case "fast":return{minClipDuration:0.25,maxCutsPerSecond:4.0,preferredCutInterval:0.5};}}private findNearestCut(sortedCuts:Cut[],time:number):Cut | null{if (sortedCuts.length===0) return null;let nearest:Cut | null=null;let minDist=Infinity;for (const cut of sortedCuts){const dist=Math.abs(cut.time-time);if (dist < minDist){minDist=dist;nearest=cut;}}return nearest;}}export function computePlanHash(plan:AIEditPlan):string{const canonical={intent:plan.intent,sourceClips:plan.sourceClips.map(c=> c.mediaId).sort(),cuts:[...plan.cuts].sort((a,b)=> a.time-b.time),transitions:[...plan.transitions].sort((a,b)=> a.atTime-b.atTime),effects:plan.effects,captions:plan.captions,musicSync:plan.musicSync,};const json=JSON.stringify(canonical,null,0);return djb2Hash(json);}export async function computePlanHashAsync(plan:AIEditPlan):Promise<string>{const canonical={intent:plan.intent,sourceClips:plan.sourceClips.map(c=> c.mediaId).sort(),cuts:[...plan.cuts].sort((a,b)=> a.time-b.time),transitions:[...plan.transitions].sort((a,b)=> a.atTime-b.atTime),effects:plan.effects,captions:plan.captions,musicSync:plan.musicSync,};const json=JSON.stringify(canonical,null,0);const encoder=new TextEncoder();const data=encoder.encode(json);const hashBuffer=await crypto.subtle.digest("SHA-256",data);const hashArray=Array.from(new Uint8Array(hashBuffer));return hashArray.map(b=> b.toString(16).padStart(2,"0")).join("");}function djb2Hash(str:string):string{let hash=5381;for (let i=0;i < str.length;i++){hash=((hash << 5)+hash)+str.charCodeAt(i);hash=hash & hash;}return Math.abs(hash).toString(16);}