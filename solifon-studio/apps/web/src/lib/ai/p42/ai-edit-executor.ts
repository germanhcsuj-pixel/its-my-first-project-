import{EditorCore}from "@/core";import type{AIEditPlan,EditDecision}from "../edit-plan";import{EditPlanValidator,type ValidatorContext,type ValidationResult}from "../edit-plan-validator";import{applyPlan,getCurrentTimelineRevision}from "../apply-plan";export interface AIEditExecutorResult{success:boolean;appliedOperationIds:string[];resultingRevision:number;decisionTrace:EditDecision[];validation:ValidationResult;error?:string;}export class AIEditExecutor{static async execute( plan:AIEditPlan,expectedRevision:number,):Promise<AIEditExecutorResult>{const editor=EditorCore.getInstance();const currentRevision=getCurrentTimelineRevision();if (expectedRevision !==currentRevision){const validator=new EditPlanValidator();const mediaLibrary=new Map<string,{duration:number}>();const assets=editor.media.getAssets();for (const asset of assets){mediaLibrary.set(asset.id,{duration:asset.duration || 0});}const validation:ValidationResult={valid:false,errors:[{code:"PLAN_OUTDATED",message:`Revision conflict:expected revision ${expectedRevision},but current timeline revision is ${currentRevision}.`,context:{expectedRevision,currentRevision},},],warnings:[],autoFixes:[],};return{success:false,appliedOperationIds:[],resultingRevision:currentRevision,decisionTrace:plan.decisions,validation,error:`Revision conflict:expected ${expectedRevision},got ${currentRevision}`,};}const mediaLibrary=new Map<string,{duration:number}>();const assets=editor.media.getAssets();for (const asset of assets){mediaLibrary.set(asset.id,{duration:asset.duration || 0});}const ctx:ValidatorContext={mediaLibrary,currentTimelineRevision:currentRevision,};const report=await applyPlan(plan,ctx);const success=report.status==="committed";const appliedOperationIds=plan.decisions.map(d=> d.id);return{success,appliedOperationIds,resultingRevision:report.resultingRevision,decisionTrace:plan.decisions,validation:report.validation ||{valid:success,errors:[],warnings:[],autoFixes:[]},error:report.error,};}}