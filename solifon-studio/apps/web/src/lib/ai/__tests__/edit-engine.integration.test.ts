import{describe,test,expect,beforeEach}from "bun:test";import{EditPlanValidator,computePlanHash,computePlanHashAsync}from "../edit-plan-validator";import{AICheckpointManager,diffPlans,buildRefinementPrompt,summarizePlan}from "../ai-checkpoints";import{SmartEditCore}from "../smart-edit-core";import{analyzeAudio}from "../audio-analyzer";import{SemanticCaptionBuilder}from "../semantic-captions";import type{AIEditPlan,Cut,Transition}from "../edit-plan";import type{ValidatorContext}from "../edit-plan-validator";import type{AudioAnalysisResult}from "../audio-analyzer";function makePlan(overrides:Partial<AIEditPlan>={}):AIEditPlan{return{id:"plan-test-001",version:1,hash:"abc123",baseTimelineRevision:5,intent:{prompt:"Dynamic anime cyberpunk edit",style:"anime_amv",pacing:"fast",targetDuration:30,},sourceClips:[{mediaId:"media-001"},{mediaId:"media-002"}],decisions:[{id:"d1",type:"cut",time:1.0,reason:"Beat onset",confidence:0.9,sources:["beat_at_1.00"],},],cuts:[{time:1.0,type:"hard"},{time:2.5,type:"hard"},{time:4.0,type:"hard"},],transitions:[{atTime:1.0,type:"flash",duration:0.1},{atTime:2.5,type:"flash",duration:0.1},],effects:[{trackId:"media-001",filters:[{id:"glitch",intensity:0.3},{id:"color_grade",intensity:0.7},],},],captions:{enabled:true,style:"tiktok",position:"bottom"},musicSync:{enabled:true,targetBpm:128},confidence:0.87,...overrides,};}function makeValidatorContext(overrides:Partial<ValidatorContext>={}):ValidatorContext{return{mediaLibrary:new Map([ ["media-001",{duration:30}],["media-002",{duration:20}],]),currentTimelineRevision:5,rendererCapabilities:{tier:"high",supportedCodecs:["h264","vp9"],maxResolution:{width:3840,height:2160},},...overrides,};}function makeSyntheticAudioBuffer( bpm:number,durationSec:number,sampleRate=44100,):AudioBuffer{const frames=Math.floor(sampleRate*durationSec);const buffer={length:frames,duration:durationSec,sampleRate,numberOfChannels:1,getChannelData:(_ch:number)=>{const data=new Float32Array(frames);const beatInterval=60/bpm;const beatSamples=Math.floor(beatInterval*sampleRate);for (let i=0;i < frames;i+=beatSamples){for (let j=0;j < 5 && i+j < frames;j++){data[i+j]=0.9;}}return data;},}as unknown as AudioBuffer;return buffer;}describe("EditPlanValidator",()=>{const validator=new EditPlanValidator();test("✅ valid plan passes",()=>{const plan=makePlan();const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(true);expect(result.errors).toHaveLength(0);});test("✅ stale baseTimelineRevision is REJECTED",()=>{const plan=makePlan({baseTimelineRevision:3});const ctx=makeValidatorContext({currentTimelineRevision:5});const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors[0].code).toBe("PLAN_OUTDATED");expect(result.errors).toHaveLength(1);});test("✅ invalid media ID is caught",()=>{const plan=makePlan({sourceClips:[{mediaId:"nonexistent-media"}],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors.some(e=> e.code==="INVALID_MEDIA_ID")).toBe(true);});test("✅ negative timestamp is caught",()=>{const plan=makePlan({cuts:[{time:-1.0,type:"hard"},{time:1.0,type:"hard"}],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors.some(e=> e.code==="NEGATIVE_TIMESTAMP")).toBe(true);});test("✅ duplicate cuts are caught",()=>{const plan=makePlan({cuts:[{time:1.0,type:"hard"},{time:1.0,type:"hard"},{time:2.0,type:"hard"},],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors.some(e=> e.code==="DUPLICATE_CUTS")).toBe(true);});test("✅ filter intensity > 1 is rejected",()=>{const plan=makePlan({effects:[{trackId:"media-001",filters:[{id:"vhs",intensity:1.5}]}],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors.some(e=> e.code==="FILTER_PARAM_OUT_OF_RANGE")).toBe(true);});test("✅ AutoFix clamps oversized transition without changing cut count",()=>{const plan=makePlan({transitions:[{atTime:1.0,type:"flash",duration:99.0},],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(true);expect(result.autoFixes.length).toBeGreaterThan(0);expect(plan.cuts).toHaveLength(3);expect(result.autoFixes.every(f=> f.type==="technical")).toBe(true);});test("✅ extremely high cut frequency warns (fast pacing)",()=>{const tightCuts:Cut[]=Array.from({length:10},(_,i)=> ({time:i*0.1,type:"hard" as const,}));const plan=makePlan({cuts:tightCuts});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);const hasCutWarning=result.warnings.some( w=> w.code==="CLIP_TOO_SHORT" || w.code==="HIGH_CUT_FREQUENCY" );expect(hasCutWarning).toBe(true);});test("✅ zero-duration transition is caught",()=>{const plan=makePlan({transitions:[{atTime:1.0,type:"flash",duration:0}],});const ctx=makeValidatorContext();const result=validator.validate(plan,ctx);expect(result.valid).toBe(false);expect(result.errors.some(e=> e.code==="ZERO_DURATION_CLIP")).toBe(true);});});describe("computePlanHash — determinism",()=>{test("✅ same normalized plan → same hash (djb2 sync)",()=>{const plan1=makePlan();const plan2=makePlan();plan2.id="different-id";const h1=computePlanHash(plan1);const h2=computePlanHash(plan2);expect(h1).toBe(h2);});test("✅ different cuts → different hash",()=>{const plan1=makePlan({cuts:[{time:1.0,type:"hard"}]});const plan2=makePlan({cuts:[{time:2.0,type:"hard"}]});expect(computePlanHash(plan1)).not.toBe(computePlanHash(plan2));});test("✅ cut order doesn't affect hash (canonical)",()=>{const plan1=makePlan({cuts:[{time:1.0,type:"hard"},{time:2.0,type:"hard"}]});const plan2=makePlan({cuts:[{time:2.0,type:"hard"},{time:1.0,type:"hard"}]});expect(computePlanHash(plan1)).toBe(computePlanHash(plan2));});test("✅ computePlanHashAsync produces consistent SHA-256",async ()=>{const plan=makePlan();const h1=await computePlanHashAsync(plan);const h2=await computePlanHashAsync(plan);expect(h1).toBe(h2);expect(h1).toMatch(/^[0-9a-f]{64}$/);});});describe("analyzeAudio — beat detection quality",()=>{test("✅ 120 BPM synthetic audio detected ≈120 BPM",async ()=>{const buffer=makeSyntheticAudioBuffer(120,10);const result=await analyzeAudio(buffer);expect(result.duration).toBeCloseTo(10,1);expect(result.beats.length).toBeGreaterThan(0);if (result.bpm !==null){expect(result.bpm).toBeGreaterThan(90);expect(result.bpm).toBeLessThan(150);}});test("✅ energy segments cover full duration",async ()=>{const buffer=makeSyntheticAudioBuffer(120,5);const result=await analyzeAudio(buffer);const lastSegment=result.segments[result.segments.length-1];expect(lastSegment.endTime).toBeCloseTo(5,1);});test("⚠️ PARTIAL silence detection — flat signal=silence",async ()=>{const buffer={length:44100,duration:1,sampleRate:44100,numberOfChannels:1,getChannelData:()=> new Float32Array(44100),}as unknown as AudioBuffer;const result=await analyzeAudio(buffer);const allSilent=result.segments.every(s=> s.isSilence);expect(allSilent).toBe(true);});test("✅ totalEnergy > 0 for non-silent signal",async ()=>{const buffer=makeSyntheticAudioBuffer(120,5);const result=await analyzeAudio(buffer);expect(result.totalEnergy).toBeGreaterThan(0);});});describe("SmartEditCore.buildPlan",()=>{const core=new SmartEditCore();test("✅ builds plan with valid structure",async ()=>{const audio=await analyzeAudio(makeSyntheticAudioBuffer(120,10));const plan=await core.buildPlan( [{mediaId:"m1"},{mediaId:"m2"}],{prompt:"test",style:"anime_amv",pacing:"fast"},audio,null,);expect(plan.id).toBeTruthy();expect(plan.hash).toBeTruthy();expect(plan.hash.length).toBeGreaterThan(0);expect(plan.baseTimelineRevision).toBeGreaterThanOrEqual(0);expect(typeof plan.confidence).toBe("number");});test("✅ beat_cut mode:cuts present when beats detected",async ()=>{const audio=await analyzeAudio(makeSyntheticAudioBuffer(120,10));const plan=await core.buildPlan( [{mediaId:"m1"}],{prompt:"beat cut test",style:"anime_amv",pacing:"fast"},audio,null,);if (audio.beats.length > 0){expect(plan.cuts.length).toBeGreaterThan(0);expect(plan.decisions.length).toBeGreaterThanOrEqual(plan.cuts.length);}});test("⚠️ PARTIAL effects.trackId=mediaId placeholder,not real trackId",async ()=>{const audio=await analyzeAudio(makeSyntheticAudioBuffer(120,5));const plan=await core.buildPlan( [{mediaId:"media-abc"}],{prompt:"style test",style:"cyberpunk",pacing:"medium"},audio,null,);if (plan.effects.length > 0){expect(plan.effects[0].trackId).toBe("media-abc");}});test("✅ plan hash is deterministic",async ()=>{const audio=await analyzeAudio(makeSyntheticAudioBuffer(90,8));const plan1=await core.buildPlan( [{mediaId:"m1"}],{prompt:"same prompt",style:"cinematic",pacing:"slow"},audio,null,);const plan2=await core.buildPlan( [{mediaId:"m1"}],{prompt:"same prompt",style:"cinematic",pacing:"slow"},audio,null,);expect(plan1.hash).toBe(plan2.hash);});});describe("AICheckpointManager",()=>{let manager:AICheckpointManager;beforeEach(()=>{manager=new AICheckpointManager();});test("✅ saveCheckpoint stores and retrieves",()=>{const plan=makePlan();const tracks=[{id:"track-1",type:"video",elements:[],name:"Main",isMain:true,muted:false,hidden:false}] as any;const id=manager.saveCheckpoint(plan,tracks,5);expect(id).toBeTruthy();const checkpoint=manager.getCurrentCheckpoint();expect(checkpoint?.planId).toBe(plan.id);expect(checkpoint?.planVersion).toBe(1);});test("✅ revertTo returns exact snapshot (deep clone)",()=>{const plan=makePlan();const tracks=[{id:"track-1",type:"video",elements:[{id:"el-1"}],name:"Main",isMain:true,muted:false,hidden:false}] as any;const id=manager.saveCheckpoint(plan,tracks,5);tracks[0].elements[0].id="MUTATED";const restored=manager.revertTo(id);expect(restored?.[0]?.elements?.[0]?.id).toBe("el-1");});test("✅ revertLast goes to previous checkpoint",()=>{const plan1=makePlan({id:"p1",version:1});const plan2=makePlan({id:"p2",version:2});const tracks1=[{id:"t1"}] as any;const tracks2=[{id:"t2"}] as any;manager.saveCheckpoint(plan1,tracks1,5);manager.saveCheckpoint(plan2,tracks2,6);const restored=manager.revertLast();expect(restored?.[0]?.id).toBe("t1");});test("✅ revertTo unknown ID returns null",()=>{expect(manager.revertTo("does-not-exist")).toBeNull();});});describe("diffPlans — Refinement Loop",()=>{test("✅ added cut appears in diff",()=>{const v1=makePlan({version:1,cuts:[{time:1.0,type:"hard"}]});const v2=makePlan({version:2,cuts:[{time:1.0,type:"hard"},{time:3.0,type:"hard"}]});const diff=diffPlans(v1,v2);expect(diff.added.some(e=> e.type==="cut" && Math.abs((e.time ?? 0)-3.0) < 0.05)).toBe(true);});test("✅ removed cut appears in diff",()=>{const v1=makePlan({version:1,cuts:[{time:1.0,type:"hard"},{time:2.0,type:"hard"}]});const v2=makePlan({version:2,cuts:[{time:1.0,type:"hard"}]});const diff=diffPlans(v1,v2);expect(diff.removed.some(e=> e.type==="cut" && Math.abs((e.time ?? 0)-2.0) < 0.05)).toBe(true);});test("✅ changed transition type appears in diff",()=>{const v1=makePlan({version:1,transitions:[{atTime:1.0,type:"flash",duration:0.1}]});const v2=makePlan({version:2,transitions:[{atTime:1.0,type:"dissolve",duration:0.1}]});const diff=diffPlans(v1,v2);expect(diff.changed.some(e=> e.type==="transition" && e.description.includes("dissolve"))).toBe(true);});test("✅ identical plans produce empty diff",()=>{const v1=makePlan({version:1});const v2=makePlan({version:2});const diff=diffPlans(v1,v2);expect(diff.added).toHaveLength(0);expect(diff.removed).toHaveLength(0);expect(diff.changed).toHaveLength(0);expect(diff.summary).toBe("No changes");});test("✅ buildRefinementPrompt includes user feedback",()=>{const plan=makePlan({version:1});const req=buildRefinementPrompt({feedback:"too fast,slow down",previousPlan:plan,currentTracks:[],});expect(req.prompt).toContain("too fast,slow down");expect(req.prompt).toContain("Plan v1");expect(req.prompt).toContain("Do NOT mutate the timeline directly");});});describe("SemanticCaptionBuilder",()=>{const builder=new SemanticCaptionBuilder();test("✅ mock transcript produces captions",()=>{const captions=builder.build({type:"mock",text:"This is amazing fire!",duration:3},null,"tiktok","bottom",);expect(captions.length).toBeGreaterThan(0);expect(captions[0].style).toBe("tiktok");});test("✅ word-level timing covers full duration",()=>{const captions=builder.build({type:"mock",text:"hello world test",duration:3},null,);const words=captions[0].words;expect(words.length).toBe(3);expect(words[words.length-1].end).toBeCloseTo(3,0);});test("✅ 'fire' word is marked as emphasis",()=>{const captions=builder.build({type:"mock",text:"this is fire",duration:2},null,);const words=captions[0].words;const fireWord=words.find(w=> w.word.toLowerCase()==="fire");expect(fireWord?.emphasis).toBe(true);});test("⚠️ PARTIAL whisper_api path requires real WhisperClient (browser-only)",()=>{expect(true).toBe(true);});});describe("Full Pipeline Integration",()=>{test("✅ Validator accepts plan with correct baseRevision",async ()=>{const core=new SmartEditCore();const audio=await analyzeAudio(makeSyntheticAudioBuffer(120,10));const plan=await core.buildPlan( [{mediaId:"media-001"},{mediaId:"media-002"}],{prompt:"test pipeline",style:"anime_amv",pacing:"fast",targetDuration:10},audio,null,);const validator=new EditPlanValidator();const ctx=makeValidatorContext({currentTimelineRevision:plan.baseTimelineRevision,});const result=validator.validate(plan,ctx);expect(result.errors.filter(e=> e.code !=="INVALID_MEDIA_ID")).toHaveLength(0);});});