import type{MusicSectionType,AudioAnalysis,MusicSection}from "./audio-analysis";import type{EditIntent,SectionOverride}from "./edit-intent";export interface IntensityKeypoint{time:number;intensity:number;section:MusicSectionType;}export interface IntensityCurve{keypoints:IntensityKeypoint[];duration:number;}const SECTION_INTENSITY:Record<MusicSectionType,number>={intro:0.2,verse:0.4,build:0.6,drop:0.95,chorus:0.85,break:0.25,outro:0.15,unknown:0.5,};export function buildIntensityCurve( audio:AudioAnalysis,intent:EditIntent,):IntensityCurve{if (audio.sections.length===0){const flatIntensity=intent.pacing==="fast" ? 0.7:intent.pacing==="slow" ? 0.3:0.5;return{keypoints:[{time:0,intensity:flatIntensity,section:"unknown"},{time:audio.duration,intensity:flatIntensity,section:"unknown"},],duration:audio.duration,};}const keypoints:IntensityKeypoint[]=[];for (const section of audio.sections){const baseIntensity=getSectionIntensity(section.type,intent.sectionOverrides);keypoints.push({time:section.startTime,intensity:baseIntensity,section:section.type,});if (section.type==="build"){const rampStart=baseIntensity*0.5;const rampEnd=baseIntensity;keypoints[keypoints.length-1].intensity=rampStart;keypoints.push({time:section.endTime,intensity:rampEnd,section:section.type,});}if (section.type==="outro"){keypoints.push({time:section.endTime,intensity:0.05,section:section.type,});}}keypoints.sort((a,b)=> a.time-b.time);return{keypoints,duration:audio.duration};}export function evaluateIntensity(curve:IntensityCurve,time:number):number{if (curve.keypoints.length===0) return 0.5;if (curve.keypoints.length===1) return curve.keypoints[0].intensity;if (time <=curve.keypoints[0].time) return curve.keypoints[0].intensity;const last=curve.keypoints[curve.keypoints.length-1];if (time >=last.time) return last.intensity;for (let i=0;i < curve.keypoints.length-1;i++){const a=curve.keypoints[i];const b=curve.keypoints[i+1];if (time >=a.time && time <=b.time){const t=(time-a.time)/(b.time-a.time);return a.intensity+t*(b.intensity-a.intensity);}}return 0.5;}export interface EditParameters{cutFrequency:number;effectIntensity:number;transitionDensity:number;zoomIntensity:number;shakeIntensity:number;glowIntensity:number;}export function intensityToEditParams( intensity:number,pacing:"slow" | "medium" | "fast",):EditParameters{const baseCutFreq=pacing==="fast" ? 2.0:pacing==="slow" ? 0.4:1.0;return{cutFrequency:baseCutFreq*(0.5+intensity*1.0),effectIntensity:intensity,transitionDensity:Math.min(1,intensity*0.8),zoomIntensity:intensity > 0.5 ? (intensity-0.5)*2:0,shakeIntensity:intensity > 0.7 ? (intensity-0.7)*3.3:0,glowIntensity:intensity > 0.6 ? (intensity-0.6)*2.5:0,};}function getSectionIntensity( section:MusicSectionType,overrides:SectionOverride[],):number{const override=overrides.find(o=> o.section===section);if (override){switch (override.intensity){case "low":return 0.2;case "medium":return 0.5;case "high":return 0.8;case "extreme":return 1.0;}}return SECTION_INTENSITY[section];}