import type{ExtendedBeat,AudioAnalysis,MusicSectionType}from "./audio-analysis";export interface BeatSelectionOptions{minStrength:number;preferDownbeats:boolean;sectionFilter:MusicSectionType | null;maxResults:number | null;minInterval:number;}export interface SelectedBeat{time:number;strength:number;isDownbeat:boolean;section:MusicSectionType;score:number;}export function selectBeatsForCuts( audio:AudioAnalysis,options:BeatSelectionOptions,):SelectedBeat[]{if (audio.beats.length===0) return [];let candidates=audio.beats.filter(b=> b.strength >=options.minStrength);if (options.sectionFilter){const sectionBeats=getBeatsInSection(candidates,audio,options.sectionFilter);candidates=sectionBeats;}const scored:SelectedBeat[]=candidates.map(beat=> ({time:beat.time,strength:beat.strength,isDownbeat:beat.isDownbeat,section:getSectionAtTime(audio,beat.time),score:computeBeatScore(beat,options),}));scored.sort((a,b)=>{const scoreDiff=b.score-a.score;if (Math.abs(scoreDiff) > 0.0001) return scoreDiff;return a.time-b.time;});const selected:SelectedBeat[]=[];for (const beat of scored){if (options.maxResults !==null && selected.length >=options.maxResults) break;const tooClose=selected.some( s=> Math.abs(s.time-beat.time) < options.minInterval );if (tooClose) continue;selected.push(beat);}selected.sort((a,b)=> a.time-b.time);return selected;}export function findNearestBeat( audio:AudioAnalysis,time:number,options:{minStrength?:number;preferDownbeat?:boolean}={},):SelectedBeat | null{const minStrength=options.minStrength ?? 0;const candidates=audio.beats.filter(b=> b.strength >=minStrength);if (candidates.length===0) return null;let best:ExtendedBeat | null=null;let bestDist=Infinity;for (const beat of candidates){const dist=Math.abs(beat.time-time);const bonus=(options.preferDownbeat && beat.isDownbeat) ? 0.1:0;const effectiveDist=dist-bonus;if (effectiveDist < bestDist){bestDist=effectiveDist;best=beat;}}if (!best) return null;return{time:best.time,strength:best.strength,isDownbeat:best.isDownbeat,section:getSectionAtTime(audio,best.time),score:computeBeatScore(best,{minStrength,preferDownbeats:options.preferDownbeat ?? false,sectionFilter:null,maxResults:null,minInterval:0,}),};}export function findStrongestBeat( audio:AudioAnalysis,windowStart:number,windowEnd:number,):SelectedBeat | null{const candidates=audio.beats.filter( b=> b.time >=windowStart && b.time <=windowEnd );if (candidates.length===0) return null;candidates.sort((a,b)=>{const strengthDiff=b.strength-a.strength;if (Math.abs(strengthDiff) > 0.0001) return strengthDiff;return a.time-b.time;});const best=candidates[0];return{time:best.time,strength:best.strength,isDownbeat:best.isDownbeat,section:getSectionAtTime(audio,best.time),score:best.strength,};}function computeBeatScore(beat:ExtendedBeat,options:BeatSelectionOptions):number{let score=beat.strength;if (options.preferDownbeats && beat.isDownbeat){score+=0.2;}if (beat.isMajor){score+=0.1;}return Math.min(1,score);}function getBeatsInSection( beats:ExtendedBeat[],audio:AudioAnalysis,sectionType:MusicSectionType,):ExtendedBeat[]{const matchingSections=audio.sections.filter(s=> s.type===sectionType);return beats.filter(b=> matchingSections.some(s=> b.time >=s.startTime && b.time <=s.endTime) );}function getSectionAtTime(audio:AudioAnalysis,time:number):MusicSectionType{for (const section of audio.sections){if (time >=section.startTime && time <=section.endTime){return section.type;}}return "unknown";}