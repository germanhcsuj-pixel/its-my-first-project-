export interface ShotBoundary{time:number;confidence:number;type:"cut" | "dissolve" | "fade" | "unknown";}export interface Shot{id:string;startTime:number;endTime:number;duration:number;motionIntensity:number;visualEnergy:number;brightness:number;saliency:number;hasFaces:boolean;hasText:boolean;}export interface SceneBoundary{time:number;confidence:number;}export interface Scene{id:string;startTime:number;endTime:number;shots:Shot[];dominantMood:"calm" | "active" | "intense" | "neutral";}export interface VideoAnalysis{duration:number;fps:number;width:number;height:number;scenes:Scene[];shots:Shot[];shotBoundaries:ShotBoundary[];sceneBoundaries:SceneBoundary[];averageMotion:number;averageBrightness:number;averageSaliency:number;}export interface RawFrameScore{time:number;motion:number;brightness:number;saliency:number;isSceneChange:boolean;hasFaces:boolean;hasText:boolean;}export function buildVideoAnalysis( frames:readonly RawFrameScore[],duration:number,fps:number,width:number,height:number,):VideoAnalysis{if (frames.length===0){return{duration,fps,width,height,scenes:[],shots:[],shotBoundaries:[],sceneBoundaries:[],averageMotion:0,averageBrightness:0,averageSaliency:0,};}const shotBoundaries:ShotBoundary[]=[];for (const frame of frames){if (frame.isSceneChange){shotBoundaries.push({time:frame.time,confidence:Math.min(1,frame.motion+0.3),type:frame.motion > 0.5 ? "cut":"dissolve",});}}const shots:Shot[]=[];const boundaries=[0,...shotBoundaries.map(b=> b.time),duration];for (let i=0;i < boundaries.length-1;i++){const startTime=boundaries[i];const endTime=boundaries[i+1];const shotFrames=frames.filter(f=> f.time >=startTime && f.time < endTime);if (shotFrames.length===0) continue;const avgMotion=shotFrames.reduce((s,f)=> s+f.motion,0)/shotFrames.length;const avgBrightness=shotFrames.reduce((s,f)=> s+f.brightness,0)/shotFrames.length;const avgSaliency=shotFrames.reduce((s,f)=> s+f.saliency,0)/shotFrames.length;const visualEnergy=Math.min(1,avgMotion*0.6+avgSaliency*0.4);shots.push({id:`shot-${i}`,startTime,endTime,duration:endTime-startTime,motionIntensity:clamp01(avgMotion),visualEnergy:clamp01(visualEnergy),brightness:clamp01(avgBrightness),saliency:clamp01(avgSaliency),hasFaces:shotFrames.some(f=> f.hasFaces),hasText:shotFrames.some(f=> f.hasText),});}const scenes:Scene[]=[];const sceneBoundaries:SceneBoundary[]=[];let currentSceneShots:Shot[]=[];let sceneIdx=0;for (let i=0;i < shots.length;i++){currentSceneShots.push(shots[i]);const isLast=i===shots.length-1;const nextShot=isLast ? null:shots[i+1];const shouldBreak=isLast || (nextShot && ( Math.abs(shots[i].brightness-nextShot.brightness) > 0.3 || Math.abs(shots[i].motionIntensity-nextShot.motionIntensity) > 0.4 ));if (shouldBreak && currentSceneShots.length > 0){const sceneStart=currentSceneShots[0].startTime;const sceneEnd=currentSceneShots[currentSceneShots.length-1].endTime;const avgMotion=currentSceneShots.reduce((s,sh)=> s+sh.motionIntensity,0)/currentSceneShots.length;scenes.push({id:`scene-${sceneIdx}`,startTime:sceneStart,endTime:sceneEnd,shots:[...currentSceneShots],dominantMood:avgMotion > 0.7 ? "intense":avgMotion > 0.4 ? "active":avgMotion > 0.15 ? "neutral":"calm",});if (!isLast && nextShot){sceneBoundaries.push({time:nextShot.startTime,confidence:0.7,});}sceneIdx++;currentSceneShots=[];}}const averageMotion=clamp01(frames.reduce((s,f)=> s+f.motion,0)/frames.length);const averageBrightness=clamp01(frames.reduce((s,f)=> s+f.brightness,0)/frames.length);const averageSaliency=clamp01(frames.reduce((s,f)=> s+f.saliency,0)/frames.length);return{duration,fps,width,height,scenes,shots,shotBoundaries,sceneBoundaries,averageMotion,averageBrightness,averageSaliency,};}function clamp01(v:number):number{return Math.max(0,Math.min(1,v));}